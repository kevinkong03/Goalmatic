<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing activity</h1>

  <%= render "form", activity: @activity %>

  <%= link_to "Show this activity", challenge_activity_path(@challenge), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to activities", challenge_activities_path(@challenge), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
